{
  "name": "zk-beacon",
  "version": "1.0.1",
  "description": "A simple ZooKeeper announce library",
  "keywords": [
    "zookeeper",
    "announce"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/metamx/zk-beacon.git"
  },
  "main": "build/index.js",
  "typescript": {
    "definition": "build/zk-beacon.d.ts"
  },
  "scripts": {
    "compile": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "build": "npm run lint && npm run compile",
    "prepublish": "npm run build",
    "test": "node_modules/.bin/mocha"
  },
  "dependencies": {
    "@types/node": "^7.0.12",
    "@types/node-zookeeper-client": "^0.2.1",
    "@types/uuid": "^2.0.29",
    "node-zookeeper-client": "^0.2.2",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "async": "^2.2.0",
    "chai": "^3.5.0",
    "coffee-script": "^1.12.4",
    "mocha": "^3.2.0",
    "tslint": "^5.0.0",
    "typescript": "^2.2.2"
  }
}
